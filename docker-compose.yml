services:
  rag-api:
    build:
      context: .
      dockerfile: backend/Dockerfile

    container_name: enterprise-rag-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - C:/Users/Lenovo/.cache/huggingface:/root/.cache/huggingface

    restart: unless-stopped